package dragonbox.common.particlesystem.initializer;


import dragonbox.common.particlesystem.Particle;
import dragonbox.common.particlesystem.emitter.Emitter;

/**
 * The initializer is designed to set initial values to particles
 * as they are created by an emitter.
 */
class Initializer
{
    public function new()
    {
    }
    
    public function initialize(emitter : Emitter, particle : Particle) : Void
    {
    }
}
