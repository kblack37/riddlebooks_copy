package dragonbox.common.command;


/**
	 * A command is some general action that will affect the current state of the program
	 * during a single update frame. You just need to subclass this and put whatever data
	 * is required for the update to be executed.
	 */
class Command
{
    public function new()
    {
    }
}
